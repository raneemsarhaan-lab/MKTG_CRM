node_modules/
build/
dist/
.next/
coverage/
.DS_Store
*.log
.env*
!.env.example
!.env.local.example

# Build artifact: regenerated by every build, and it conflicted on every
# merge because it is tracked. It is output, not source.
tsconfig.tsbuildinfo
